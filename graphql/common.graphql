type Pagination {
  offset: Int
  limit: Int
}

enum Sort {
  RELEVANT
  POPULAR
  NEW
}

enum Visibility {
  PUBLIC
  FRIENDS
  PRIVATE
}

type Error {
  message: String!
}

scalar FileUpload

type File {
  filename: String!
  mimetype: String!
  encoding: String!
}
